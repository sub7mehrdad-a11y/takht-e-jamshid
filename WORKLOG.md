<div dir="rtl">

# کارنامه‌ی کار — تخت جمشید

هر ورودی از بالا به پایین، تازه‌ترین اول.

---

## ۲۶ اوت ۲۰۲۶ — جوابِ آنی برای هومان و رستم

**کامیت:** `bbad9db`

هر دو دقیقاً مثلِ استعلامِ افراسیاب سرِ میز **از گرداننده** پرسیده می‌شوند: بازیکن هدفش را
با دست نشان می‌دهد و منتظرِ علامتِ گرداننده می‌ماند. قبلاً گرداننده نتیجه را فقط
**بعد از حلِ کلِ شب** می‌فهمید که برای واکنشِ همان لحظه خیلی دیر بود.

| کارت | حالت‌هایی که پوشش می‌دهد |
|---|---|
| **هومان** | حدس غلط / حدس درست روی ضحاکی (+ نامِ قابلیتِ کپی‌شده) / حدس درست روی ارمایل (بی‌اثر) / حدس درست روی سایر ایرانی‌ها |
| **رستم** | تیرِ درست / اشتباه با از‌دست‌دادنِ زره / اشتباه بدونِ زره (خودش می‌میرد) |

### چرا این پیش‌بینی‌ها قابلِ اتکا هستند
**سودابه در ترتیبِ بیدارشدن قبل از هومان و رستم ثبت می‌شود.** پس وقتی نوبتِ آن‌ها
می‌رسد، افسونِ امشب از قبل در کنسول ثبت شده و می‌توانیم باطل‌بودنِ قابلیت را درست
حساب کنیم. اگر گرداننده هدفِ سودابه را عوض کند، کارت‌های وابسته دوباره حساب می‌شوند.

> **نکته‌ی بازی‌سازی:** وقتی قابلیت به‌خاطرِ افسون باطل است، کارت به گرداننده می‌گوید
> «طوری رفتار کن که خودش نفهمه افسون شده». وگرنه بازیکن از سکوتِ گرداننده می‌فهمد.

> **نکته‌ی دوم:** جواب‌ها بینِ «چیزی که همین حالا می‌شود گفت» و «چیزی که تا صبح صبر
> می‌کند» تفکیک شده‌اند. مثلاً به هومان گفته می‌شود حدسش درست بود، **نه** اینکه هدف مُرد.

### تست‌ها
از ۳۸ به **۴۵**. هفت تستِ تازه، هر ادعای روی کارت را با رفتارِ واقعیِ موتور قفل می‌کنند.
دو تای آن‌ها عمداً روی حالت‌هایی هستند که راحت خراب می‌شوند:
**تیرِ اشتباه هیچ‌وقت هدفِ ایرانی را نمی‌کشد** و **همدستی که کپی شده کشته نمی‌شود**.
اگر موتور روزی تغییر کند، بدونِ این تست‌ها گرداننده یک شبِ کامل به بازیکن‌ها اطلاعاتِ
غلط می‌داد.

---

## ۲۶ اوت ۲۰۲۶ — استعلامِ ارمایل + جوابِ آنیِ استعلام

**کامیت:** `e91d71d`

### ۱. ارمایل باید در استعلام «ضحاکی» نشان داده شود
کد او را «ضحاکی نیست» گزارش می‌کرد. سطرِ ۷۸ سند می‌گوید در **همه‌ی استعلام‌ها** ضحاکی
نشان داده می‌شود، با اینکه در شمارشِ پیروزی ایرانی است. **همین دوگانگی کلِ فلسفه‌ی
این نقش است** — گزارشِ صادقانه، تله‌ای که ارمایل باید باشد را از بین می‌برد.

تابعِ `appearsZahhakiInInquiry()` در موتور این را تصمیم می‌گیرد.

> ⚠️ **این تابع عمداً فقط برای استعلام است.** این استتار نباید به شمارشِ برد،
> هدف‌گیریِ ضحاک، یا حدسِ هومان نشت کند — آنجاها `side` واقعی ملاک است.
> دو تست این را قفل کرده‌اند: ارمایل ضحاکی خوانده می‌شود، ولی هم‌زمان
> برای پیروزی ایرانی می‌ماند و مصونیتش در برابر هومان از قاعده‌ی خودش می‌آید نه از استتار.

### ۲. جوابِ استعلام باید همان لحظه بیاید
استعلام سرِ میز **از خودِ گرداننده** پرسیده می‌شود، پس گرداننده باید جواب را همان
لحظه‌ی ثبتِ انتخابِ افراسیاب ببیند تا به او علامت بدهد. قبلاً جواب فقط **بعد از حلِ کلِ
شب** در گزارش می‌آمد که عملاً بی‌فایده بود.

حالا به‌محضِ انتخابِ هدف، روی کارت می‌آید: `<اسم> → ضحاکی است / ضحاکی نیست`.

> کارت جواب را از **همان تابعِ موتور** می‌خواند که خودِ resolve استفاده می‌کند، تا
> جوابِ آنی و نتیجه‌ی ثبت‌شده هیچ‌وقت از هم جدا نیفتند.

### تست‌ها
از ۳۳ به **۳۸**.

---

## ۲۶ اوت ۲۰۲۶ — شرطِ پایانِ بازی + سقفِ افسونِ سودابه

**کامیت:** `764a587`

### ۱. بازی هیچ‌وقت تمام نمی‌شد
ستونِ `winner_side` در دیتابیس وجود داشت و در کلِ اپ **صفر بار** استفاده شده بود. یعنی
حتی اگر همه‌ی ضحاکیان می‌مردند، نرم‌افزار هیچ واکنشی نشان نمی‌داد.

`tjEvaluateWin` یارانِ زنده‌ی هر طرف را می‌شمارد و `tjEndGame` نتیجه را می‌نویسد.

> **سه ظرافتِ شمارش که حتماً باید درست باشد** (اگر روزی این کد را تغییر دادی، این‌ها را نشکن):
> - **جمشید و ضحاک شمرده نمی‌شوند** چون نامیرا هستند. اگر شمرده شوند، هیچ طرفی هرگز
>   به صفر نمی‌رسد و بازی باز هم تمام نمی‌شود.
> - **ارمایل جزو ایرانی‌ها** حساب می‌شود (سطرِ ۷۸ سند) با اینکه در استعلام‌ها ضحاکی
>   نشان داده می‌شود. چون `side` او در چیدمان‌ها `jamshidi` است، خودبه‌خود درست است.
> - **سهرابِ بی‌طرف** برای هیچ طرفی شمرده نمی‌شود (بخشِ ۱ سند).

بعد از **هر حذف** (روز و شب) چک می‌شود. با برد: بازی `ended` می‌شود، پولینگ متوقف
می‌شود، و صفحه‌ی پایان **نقشِ همه را آشکار می‌کند**. گرداننده و بازیکن هر دو به آن صفحه
می‌رسند — حتی بعد از رفرش، چون تشخیص از روی `games.status` است نه وضعیتِ داخلِ حافظه.

### ۲. سودابه سقف نداشت
سند دو بار در کلِ بازی اجازه می‌دهد؛ هیچ‌جا اعمال نشده بود و عملاً **هر شب** می‌توانست افسون کند.

- موتور حالا هر استفاده یک شارژ خرج می‌کند و بعد از دو بار اکشن را رد می‌کند.
  **سقف در موتور است نه فقط در UI**، تا حتی با دور زدنِ کنسول هم قانون بشکند.
- شمارنده (`بارِ N از ۲`) طبقِ خواسته‌ی گرداننده **فقط سمتِ گرداننده** نمایش داده
  می‌شود، چون سودابه انتخابش را با دست به گرداننده علامت می‌دهد و داشبوردِ مستقلی ندارد.
  بعد از خرجِ هر دو شارژ، کارتش اصلاً نمایش داده نمی‌شود.

### تست‌ها
از ۲۴ به **۳۳** رسید. موارد تازه:
- خرجِ شارژ ۱ و ۲، و ردشدنِ سومی
- **نکته‌ی ظریف:** وقتی افسون رد می‌شود، هدفش نباید بلاک شود — تست می‌کند که هومانِ
  رد‌شده از افسون، حدسِ درستش کار کند
- شمارشِ برد شاملِ حالتِ ارمایل، سهرابِ بی‌طرف، و کنارگذاشتنِ گرداننده

نوشتنِ `status='ended'` و `winner_side` روی دیتابیسِ واقعی هم تست و پاک شد.

### آنچه عمداً انجام نشد
طبقِ توضیحِ گرداننده، **شبِ معارفه** و **کارت‌های زندان و اهریمنِ ضحاک** حضوری سرِ میز
اجرا می‌شوند (بازیکن چشم باز می‌کند و با انگشت نشان می‌دهد؛ گرداننده زندانی را به اتاقِ
دیگر می‌برد). این‌ها **نقصِ نرم‌افزار نیستند** و نباید در فهرستِ کارهای باقی‌مانده بیایند.

---

## ۲۶ اوت ۲۰۲۶ — پیاده‌سازیِ مکانیزمِ سروش (نامه‌نگاری)

**کامیت:** `330b072`

### مسئله
سروش فقط یک **فلگ** بود: کارتِ جمشید مقدارِ `soroush_active_until_night` را ست می‌کرد و
**هیچ‌جا خوانده نمی‌شد**. جدولِ `letters` و یک پروتوتایپِ مستقل وجود داشتند ولی به هیچ‌چیز
وصل نبودند. عملاً هیچ نامه‌ای نه نوشته می‌شد نه تحویل.

### جریانِ خواسته‌شده
۱. گرداننده هر وقت خواست پنجره را باز می‌کند
۲. همه می‌توانند به همه نامه بدهند
۳. نامه‌ها **بی‌نام**اند
۴. تحویل در **پایانِ شب** با دستورِ گرداننده
۵. نامه‌های **شنودشده** به ضحاک هم می‌رسند

### `supabase-client.js`
`tjActivateSoroush` (بازکردنِ پنجره، با شماره‌ی روزِ فعال‌شدن)، `tjSendLetter`،
`tjMySentLetters`، `tjMyInbox`، `tjGetLetters`، `tjDeliverLetters`،
`tjInterceptLetters`، `tjInterceptedLetters`.

> **نکته‌ی مهمِ طراحی — بی‌نامی در لایه‌ی کوئری اعمال می‌شود، نه در نمایش.**
> کوئریِ صندوقِ ورودیِ گیرنده و کوئریِ شنودِ ضحاک، ستونِ `sender_id` را **اصلاً select
> نمی‌کنند**. یعنی نامِ فرستنده هیچ‌وقت به کلاینتی که نباید ببیندش فرستاده نمی‌شود.
> `sender_id` همچنان در دیتابیس ذخیره می‌شود تا گرداننده لاگ داشته باشد.

> **تله‌ای که رفع شد:** هنگامِ تغییرِ فردِ شنودشده، اول فلگ‌های قبلی پاک می‌شوند؛ وگرنه
> شنودها روی هم انباشته می‌شدند و ضحاک نامه‌های هر دو نفر را می‌دید.

### `index.html`
- رابطِ نامه یک **نوارِ شناور + مودال** است نه یک صفحه‌ی جدا — چون بازیکن بسته به نقش و
  فاز روی یکی از **چهار صفحه‌ی مختلف** است و نامه باید از همه‌جا در دسترس باشد.
  به `body` هم پدینگِ پایین اضافه می‌شود تا نوار، دکمه‌ی آخرِ صفحه را نپوشاند.
- `refreshSoroush` **قبل از** early-returnِ `lastRenderedKey` در پولینگ صدا زده می‌شود،
  چون بازشدنِ پنجره و رسیدنِ نامه مستقل از تغییرِ فازند.
- متنِ نامه‌ها قبل از درج در HTML **escape** می‌شود؛ متنِ آزادی است که بازیکن‌ها تایپ
  می‌کنند و مستقیم داخلِ صفحه‌ی بازیکنِ دیگر می‌رود.
- گرداننده: دکمه‌ی فراخوانی در کنترلِ فاز، و داخلِ کنسولِ شب انتخابگرِ شنود + دکمه‌ی تحویل.

### تست‌ها — `tests/letters.live-test.js`
۱۴ بررسی روی **دیتابیسِ واقعی**. اجرا:

```
node tests/letters.live-test.js
```

پوشش: ذخیره‌ی نامه، خالی‌بودنِ صندوق پیش از تحویل، شنود روی هر دو سمت (فرستاده و
گرفته)، پاک‌شدنِ فلگ‌های قبلی هنگامِ تغییرِ هدفِ شنود، تحویل، بی‌اثربودنِ تحویلِ دوم، و
**تأییدِ اینکه هر دو کوئریِ بی‌نام واقعاً `sender_id` را برنمی‌گردانند**.
تست بازیِ آزمایشیِ خودش را می‌سازد و در پایان همه را پاک می‌کند (تأیید شد: ۲۹ بازی، صفر نامه‌ی یتیم).

### سه اختلاف با بخشِ ۷ سند — همه طبقِ خواسته‌ی کاربر
| موضوع | سند | پیاده‌سازی |
|---|---|---|
| فراخوان | جمشید | **گرداننده** (کارتِ جمشید هم دست‌نخورده کار می‌کند) |
| فرستنده | نامه‌ی روز علنی با نامِ فرستنده | **بی‌نام** |
| مخاطب | نامه‌ی شب خطاب به **نقش** | خطاب به **شخص** |

### محدودیتِ شناخته‌شده (از قبل موجود، نه ناشی از این تغییر)
روی جدولِ `letters` هیچ RLS فعال نیست، پس یک بازیکنِ کنجکاو می‌تواند مستقیم با API
همه‌ی نامه‌ها را بخواند. این ویژگیِ کلِ معماری است (جدولِ `players` هم همین‌طور است و
نقش‌ها را لو می‌دهد) و مدلِ اعتمادِ بازی بر پایه‌ی گرداننده‌ی انسانی است. اگر روزی مهم شد،
باید RLS + توکنِ نشست اضافه شود.

### کارِ باقی‌مانده
- سند دو استثنا دارد که **عمداً پیاده نشد** چون کاربر گفت «همه»: سهراب در شبِ سروش
  نباید نامه بدهد، و ارمایل بعد از ناتوانیِ ضحاک نباید بتواند. اگر خواستی اضافه می‌شود.
- هر بازیکن در هر پنجره فقط **یک** نامه می‌تواند بفرستد (طبقِ سند).

---

## ۲۶ اوت ۲۰۲۶ — ادغامِ فازِ شب در اپ

**کامیت:** `1a5da3a`

### مسئله
موتورِ حلِ شب و کنسولِ شبانه **هر دو از قبل نوشته شده بودند** ولی بیرونِ اپِ در حالِ اجرا
بودند و هیچ‌وقت صدا زده نمی‌شدند:

- `resolveNight.js` یک ماژولِ ESM بود که هیچ‌جا `import` نمی‌شد.
- `host-night-console-prototype.html` یک پروتوتایپِ مستقل با بازیکن‌های نمونه‌ی hardcode بود.
- داخلِ `index.html` فازِ شب فقط یک صفحه‌ی ایستای «شب است» برای بازیکن و یک جعبه‌ی
  اطلاعاتِ جاماسپ برای گرداننده بود.

نتیجه: هیچ اکشنِ شبانه‌ای ثبت نمی‌شد و هیچ شبی حل نمی‌شد.

### `resolveNight.js`
کلمه‌ی `export` حذف شد. `index.html` هیچ بیلدی ندارد و با `<script src>` ساده لود می‌کند،
پس موتور باید توابع را سراسری تعریف کند.

**همین فایل منبعِ حقیقت شد، نه کپیِ داخلِ پروتوتایپ** — چون نسخه‌ی کامل‌تری است و دو
قاعده را دارد که کپیِ پروتوتایپ نداشت: شکستنِ پیوندِ بیژن‌ومنیژه، و قاعده‌ی دو-تلاشیِ گرسیوز.

### `supabase-client.js`
- `tjSubmitNightAction` / `tjDeleteNightAction` / `tjGetNightActions` برای لاگِ خامِ `night_actions`
- `tjApplyNightResolve` برای ماندگارکردنِ نتیجه: کشته‌ها، `state_flags`ِ تغییرکرده، فلگ‌های
  هنگامه‌ی بازی، و `events_log`
- ثبتِ اکشن به‌صورت **upsert** روی کلیدِ یکتای اسکیما
  `(game_id, night_number, actor_player_id, action_type)` انجام می‌شود تا اگر گرداننده
  نظرش عوض شد، رکورد به‌جای تکرار **به‌روزرسانی** شود.
  ✅ روی دیتابیسِ واقعی تست شد: رکورد در جا آپدیت شد و تعداد ردیف دقیقاً ۱ ماند.
- `zahhak_hungry_streak` داخلِ همان `hengameh_flags` (jsonb) ذخیره می‌شود. موتور آن را در
  سطحِ بالا می‌خواهد ولی جدولِ `games` چنین ستونی ندارد؛ این کار **مهاجرتِ دستیِ اسکیما را
  از گردنِ کاربر برمی‌دارد**.
- شماره‌گذاریِ شب از قراردادِ بقیه‌ی اپ پیروی می‌کند (شبِ N زیرِ `day_number`)؛ ستونِ
  بی‌استفاده‌ی `games.night_number` دست‌نخورده ماند تا `tjGetEliminatedIn` و اطلاعاتِ
  جاماسپ نشکنند.

### `index.html`
- لودِ `resolveNight.js`. قبلش بررسی شد که هیچ‌کدام از سراسری‌هایش
  (`resolveNight`, `findPlayer`, `actionOf`) با نام‌های موجود تداخل ندارند.
- پنلِ شبِ گرداننده به یک **کنسولِ واقعی** تبدیل شد: یک کارت برای هر نقشِ شبانه به‌ترتیبِ
  بیدارشدن، نوارِ پیشرفت، دکمه‌ی «رد شو» برای هر کارت، و دکمه‌ی حلِ شب که تا ثبت یا
  ردشدنِ همه‌ی نقش‌ها غیرفعال می‌ماند.
- کارتِ ارمایل **فقط همان دو نفری** را نشان می‌دهد که ضحاک واقعاً انتخاب کرده.
- اکشن‌های کشنده هرگز جمشید و ضحاک را در فهرستِ هدف نمی‌آورند (نامیرا هستند).
- توانایی‌های یک‌بارمصرفی که قبلاً خرج شده‌اند (پرِ سیمرغِ زال، فداکاریِ گرسیوز) اصلاً کارت نمی‌گیرند.
- پنل **فقط وقتی شماره‌ی شب عوض شود** بازسازی می‌شود، وگرنه پولینگِ هر ۲ ثانیه وسطِ کار
  انتخاب‌ها را پاک می‌کرد. ضمناً اکشن‌های قبلاً ثبت‌شده را برمی‌گرداند تا رفرشِ صفحه شب را از بین نبرد.
- نتیجه دو تکه شد: **گزارشِ خصوصی** با علت‌ها و رویدادها برای گرداننده، و **خطِ اعلامِ عمومی**
  که فقط اسمِ کشته‌ها را می‌گوید — مثلِ همان الگویی که فازِ روز برای مخفی‌نگه‌داشتنِ مکانیزم دارد.

### تست‌ها — `tests/resolveNight.test.js`
۲۴ سناریو روی موتور. اجرا:

```
node tests/resolveNight.test.js
```

> **دو تستِ اولیه رد شد و هر دو اشتباهِ خودِ تست بود، نه باگِ موتور.** هر دو با سند تأیید شد:
> ارمایل در برابرِ افسونِ سودابه **مصون** است (سطرِ ۶۹ سند)، و زرهِ رستم کشتنِ ضحاک را
> **جذب** می‌کند (سطرِ ۸۴). این نکته اینجا ثبت شده تا دفعه‌ی بعد دوباره اشتباه گرفته نشود.

سوئیت حالا مثالِ عملیِ خودِ سند (سطرِ ۱۵۵) را هم دارد: هومانِ افسون‌شده با حدسِ درست هم
نباید رستم را بکشد.

### تأییدِ روی دیتابیسِ واقعی
یک بازی + ۳ بازیکنِ آزمایشی ساخته شد، `night_actions` و `events_log` نوشته شد، رفتارِ
upsert بررسی شد، و بعد **همه‌ی داده‌های آزمایشی پاک شدند**. تعدادِ بازی‌ها دقیقاً به
همان ۲۹ تای قبل برگشت.

### کارِ باقی‌مانده
- نقش‌های شبانه‌ای که هنوز کارت ندارند: پیران، ارجاسب، اسفندیار، گردآفرید، سهراب.
- مکانیزمِ سروش (`letters`) هنوز فقط پروتوتایپ است.
- فازهای `nimrooz` و `resolution_choice` هنوز داخلِ اپ نیستند.

---

## ۲۶ اوت ۲۰۲۶ — رفعِ «دکمه‌ی ورود کار نمی‌کند» + مرئی‌کردنِ خطاهای بک‌اند

**کامیت:** `96e5d55`

### مشکلی که گزارش شد
در صفحه‌ی ورود، بعد از واردکردنِ نامِ گرداننده و کلیک روی دکمه‌ی «ورود به بازی»،
هیچ اتفاقی نمی‌افتاد — نه خطایی، نه تغییری در صفحه.

### ریشه‌ی واقعی: باگِ فرانت‌اند نبود
**پروژه‌ی Supabase به‌خاطرِ ۷ روز بی‌فعالیتی خودکار pause شده بود.**

روالِ عیب‌یابی:

۱. **اول فرانت‌اند تبرئه شد.** فایلِ `assets/entry-screen.jpg` پیکسل‌به‌پیکسل اسکن شد:
   نوارِ طلاییِ دکمه در خودِ تصویر بینِ ۶۰.۷٪ تا ۶۵.۲٪ ارتفاع است، و هات‌اسپاتِ
   نامرئیِ `#createBtn` در CSS روی `top:60.5%; height:4.6%` نشسته — یعنی دقیقاً منطبق.
   نسبتِ ابعادِ `aspect-ratio:853/1844` هم مو‌به‌مو با ابعادِ واقعیِ تصویر (853×1844) یکی است.
   نتیجه: کلیک همیشه به `handleCreate()` می‌رسید.
۲. اسکریپت `type="module"` نیست، پس `onclick` سراسری سالم است. کتابخانه‌ی unpkg هم در دسترس بود.
   پالیسی‌های RLS در `schema.sql` هم باز و درست‌اند.
۳. **تستِ مستقیمِ DNS:** دامنه‌ی `wdzfngsszluwepgomstv.supabase.co` از ریزالورِ عمومیِ
   گوگل `Status:3` (NXDOMAIN) می‌داد، در حالی که خودِ `supabase.co` نرمال جواب می‌داد.
۴. بعد از اینکه کاربر داشبورد را باز کرد، پروژه شروع به restore شدن کرد:
   `521` → `404` → `503` → **`200`** (حدودِ ۷۵ ثانیه).

> **نکته‌ی مهم برای دفعه‌ی بعد:** تشخیصِ اولیه «پروژه حذف شده» **غلط** بود.
> Supabase رکوردِ DNS پروژه‌های *pause شده* را هم برمی‌دارد، پس NXDOMAIN دلیلِ حذف نیست.
> داده‌ها سرِ جایشان ماندند (۲۹ بازی از ۲ تا ۱۱ اوت هنوز در دیتابیس هستند) و
> نه کلیدها عوض شدند، نه `schema.sql` دوباره اجرا شد.

### تأییدِ نهایی
هر ۷ جدول (`games`, `players`, `roles`, `night_actions`, `day_votes`, `letters`,
`events_log`) با `HTTP 200` جواب دادند. سپس **همان دو `INSERT`ی که `tjCreateGame()`
می‌زند** سرتاسری تست شد (ساختِ اتاق + ثبتِ گرداننده) — هر دو موفق. رکوردهای تستی
بلافاصله پاک شدند (`HTTP 204`) و دیتابیس دست‌نخورده ماند.

جدولِ `roles` خالی است ولی بی‌ضرر — اپ اصلاً از آن کوئری نمی‌گیرد و نقش‌ها از
دیتای inline داخلِ `index.html` می‌آیند.

### تغییراتی که زده شد
هدف: اینکه دفعه‌ی بعد یک بک‌اندِ خواب، شبیهِ یک دکمه‌ی خراب به‌نظر نرسد.

**`supabase-client.js`**
- گاردِ صریح روی `window.supabase`. بدونِ آن، اگر CDN فیلتر/قطع باشد کلِ فایل در
  همان خط می‌ترکد و **همه‌ی** توابعِ `tj*` بی‌صدا تعریف‌نشده می‌مانند — یعنی تمامِ
  دکمه‌ها بی‌اثر می‌شوند بدونِ هیچ نشانه‌ای.
- تمامِ درخواست‌ها از یک `fetch` wrapper با تایم‌اوتِ ۱۵ ثانیه‌ای (`AbortController`)
  عبور می‌کنند تا بک‌اندِ در‌دسترس‌نبودن، سریع و با پیغامِ فارسی شکست بخورد نه با انتظارِ بی‌پایان.

**`index.html`**
- افزودنِ نوارِ وضعیتِ شناور (`.busy-box`) با پیغامِ «در حالِ ساختِ اتاق…».
- تبدیلِ `.err-box` به توستِ `position:fixed` با پس‌زمینه‌ی مات. دلیل: صفحه‌ی ورود
  یک تصویرِ بسیار بلند است و کادرِ خطا در جریانِ عادیِ صفحه، روی موبایل از کادرِ دید بیرون می‌افتاد.
- حذفِ نوشتنِ `textContent` روی `#createBtn` و `#joinBtn`. این دو هات‌اسپاتِ **نامرئی**
  روی تصویرند و تزریقِ متن در آن‌ها طرح را خراب می‌کرد.
- افزودنِ `tjBackendReady()` به‌عنوانِ پیش‌بررسی، تا نبودِ فایلِ کلاینت خودش را گزارش کند.

هر دو فایل با `node --check` سینتکس‌چک شدند.

### کارهای باز
- **ادغامِ فازِ شب:** موتورِ کاملِ `resolveNight.js` و کنسولِ کاملِ
  `host-night-console-prototype.html` هر دو نوشته شده‌اند ولی هنوز داخلِ `index.html`
  فراخوانی نمی‌شوند (آنجا فعلاً فقط `screen-night-wait` ایستا و پنلِ جاماسپ هست).
  سه قدمِ لازم: انتقالِ کنسول به `index.html`، تبدیلِ `resolveNight.js` از ESM به
  اسکریپتِ سراسری، و نوشتنِ اکشن‌ها در `night_actions` + ذخیره‌ی خروجی در `events_log`.
- **جلوگیری از pause شدنِ دوباره:** پروژه‌های رایگانِ Supabase بعد از ۷ روز
  بی‌فعالیتی خودکار pause می‌شوند.

</div>
