<div dir="rtl">

# کارنامه‌ی کار — تخت جمشید

هر ورودی از بالا به پایین، تازه‌ترین اول.

---

## ۲۶ اوت ۲۰۲۶ — ادغامِ فازِ شب در اپ

**کامیت:** `1a5da3a`

### مسئله
موتورِ حلِ شب و کنسولِ شبانه **هر دو از قبل نوشته شده بودند** ولی بیرونِ اپِ در حالِ اجرا
بودند و هیچ‌وقت صدا زده نمی‌شدند:

- `resolveNight.js` یک ماژولِ ESM بود که هیچ‌جا `import` نمی‌شد.
- `host-night-console-prototype.html` یک پروتوتایپِ مستقل با بازیکن‌های نمونه‌ی hardcode بود.
- داخلِ `index.html` فازِ شب فقط یک صفحه‌ی ایستای «شب است» برای بازیکن و یک جعبه‌ی
  اطلاعاتِ جاماسپ برای گرداننده بود.

نتیجه: هیچ اکشنِ شبانه‌ای ثبت نمی‌شد و هیچ شبی حل نمی‌شد.

### `resolveNight.js`
کلمه‌ی `export` حذف شد. `index.html` هیچ بیلدی ندارد و با `<script src>` ساده لود می‌کند،
پس موتور باید توابع را سراسری تعریف کند.

**همین فایل منبعِ حقیقت شد، نه کپیِ داخلِ پروتوتایپ** — چون نسخه‌ی کامل‌تری است و دو
قاعده را دارد که کپیِ پروتوتایپ نداشت: شکستنِ پیوندِ بیژن‌ومنیژه، و قاعده‌ی دو-تلاشیِ گرسیوز.

### `supabase-client.js`
- `tjSubmitNightAction` / `tjDeleteNightAction` / `tjGetNightActions` برای لاگِ خامِ `night_actions`
- `tjApplyNightResolve` برای ماندگارکردنِ نتیجه: کشته‌ها، `state_flags`ِ تغییرکرده، فلگ‌های
  هنگامه‌ی بازی، و `events_log`
- ثبتِ اکشن به‌صورت **upsert** روی کلیدِ یکتای اسکیما
  `(game_id, night_number, actor_player_id, action_type)` انجام می‌شود تا اگر گرداننده
  نظرش عوض شد، رکورد به‌جای تکرار **به‌روزرسانی** شود.
  ✅ روی دیتابیسِ واقعی تست شد: رکورد در جا آپدیت شد و تعداد ردیف دقیقاً ۱ ماند.
- `zahhak_hungry_streak` داخلِ همان `hengameh_flags` (jsonb) ذخیره می‌شود. موتور آن را در
  سطحِ بالا می‌خواهد ولی جدولِ `games` چنین ستونی ندارد؛ این کار **مهاجرتِ دستیِ اسکیما را
  از گردنِ کاربر برمی‌دارد**.
- شماره‌گذاریِ شب از قراردادِ بقیه‌ی اپ پیروی می‌کند (شبِ N زیرِ `day_number`)؛ ستونِ
  بی‌استفاده‌ی `games.night_number` دست‌نخورده ماند تا `tjGetEliminatedIn` و اطلاعاتِ
  جاماسپ نشکنند.

### `index.html`
- لودِ `resolveNight.js`. قبلش بررسی شد که هیچ‌کدام از سراسری‌هایش
  (`resolveNight`, `findPlayer`, `actionOf`) با نام‌های موجود تداخل ندارند.
- پنلِ شبِ گرداننده به یک **کنسولِ واقعی** تبدیل شد: یک کارت برای هر نقشِ شبانه به‌ترتیبِ
  بیدارشدن، نوارِ پیشرفت، دکمه‌ی «رد شو» برای هر کارت، و دکمه‌ی حلِ شب که تا ثبت یا
  ردشدنِ همه‌ی نقش‌ها غیرفعال می‌ماند.
- کارتِ ارمایل **فقط همان دو نفری** را نشان می‌دهد که ضحاک واقعاً انتخاب کرده.
- اکشن‌های کشنده هرگز جمشید و ضحاک را در فهرستِ هدف نمی‌آورند (نامیرا هستند).
- توانایی‌های یک‌بارمصرفی که قبلاً خرج شده‌اند (پرِ سیمرغِ زال، فداکاریِ گرسیوز) اصلاً کارت نمی‌گیرند.
- پنل **فقط وقتی شماره‌ی شب عوض شود** بازسازی می‌شود، وگرنه پولینگِ هر ۲ ثانیه وسطِ کار
  انتخاب‌ها را پاک می‌کرد. ضمناً اکشن‌های قبلاً ثبت‌شده را برمی‌گرداند تا رفرشِ صفحه شب را از بین نبرد.
- نتیجه دو تکه شد: **گزارشِ خصوصی** با علت‌ها و رویدادها برای گرداننده، و **خطِ اعلامِ عمومی**
  که فقط اسمِ کشته‌ها را می‌گوید — مثلِ همان الگویی که فازِ روز برای مخفی‌نگه‌داشتنِ مکانیزم دارد.

### تست‌ها — `tests/resolveNight.test.js`
۲۴ سناریو روی موتور. اجرا:

```
node tests/resolveNight.test.js
```

> **دو تستِ اولیه رد شد و هر دو اشتباهِ خودِ تست بود، نه باگِ موتور.** هر دو با سند تأیید شد:
> ارمایل در برابرِ افسونِ سودابه **مصون** است (سطرِ ۶۹ سند)، و زرهِ رستم کشتنِ ضحاک را
> **جذب** می‌کند (سطرِ ۸۴). این نکته اینجا ثبت شده تا دفعه‌ی بعد دوباره اشتباه گرفته نشود.

سوئیت حالا مثالِ عملیِ خودِ سند (سطرِ ۱۵۵) را هم دارد: هومانِ افسون‌شده با حدسِ درست هم
نباید رستم را بکشد.

### تأییدِ روی دیتابیسِ واقعی
یک بازی + ۳ بازیکنِ آزمایشی ساخته شد، `night_actions` و `events_log` نوشته شد، رفتارِ
upsert بررسی شد، و بعد **همه‌ی داده‌های آزمایشی پاک شدند**. تعدادِ بازی‌ها دقیقاً به
همان ۲۹ تای قبل برگشت.

### کارِ باقی‌مانده
- نقش‌های شبانه‌ای که هنوز کارت ندارند: پیران، ارجاسب، اسفندیار، گردآفرید، سهراب.
- مکانیزمِ سروش (`letters`) هنوز فقط پروتوتایپ است.
- فازهای `nimrooz` و `resolution_choice` هنوز داخلِ اپ نیستند.

---

## ۲۶ اوت ۲۰۲۶ — رفعِ «دکمه‌ی ورود کار نمی‌کند» + مرئی‌کردنِ خطاهای بک‌اند

**کامیت:** `96e5d55`

### مشکلی که گزارش شد
در صفحه‌ی ورود، بعد از واردکردنِ نامِ گرداننده و کلیک روی دکمه‌ی «ورود به بازی»،
هیچ اتفاقی نمی‌افتاد — نه خطایی، نه تغییری در صفحه.

### ریشه‌ی واقعی: باگِ فرانت‌اند نبود
**پروژه‌ی Supabase به‌خاطرِ ۷ روز بی‌فعالیتی خودکار pause شده بود.**

روالِ عیب‌یابی:

۱. **اول فرانت‌اند تبرئه شد.** فایلِ `assets/entry-screen.jpg` پیکسل‌به‌پیکسل اسکن شد:
   نوارِ طلاییِ دکمه در خودِ تصویر بینِ ۶۰.۷٪ تا ۶۵.۲٪ ارتفاع است، و هات‌اسپاتِ
   نامرئیِ `#createBtn` در CSS روی `top:60.5%; height:4.6%` نشسته — یعنی دقیقاً منطبق.
   نسبتِ ابعادِ `aspect-ratio:853/1844` هم مو‌به‌مو با ابعادِ واقعیِ تصویر (853×1844) یکی است.
   نتیجه: کلیک همیشه به `handleCreate()` می‌رسید.
۲. اسکریپت `type="module"` نیست، پس `onclick` سراسری سالم است. کتابخانه‌ی unpkg هم در دسترس بود.
   پالیسی‌های RLS در `schema.sql` هم باز و درست‌اند.
۳. **تستِ مستقیمِ DNS:** دامنه‌ی `wdzfngsszluwepgomstv.supabase.co` از ریزالورِ عمومیِ
   گوگل `Status:3` (NXDOMAIN) می‌داد، در حالی که خودِ `supabase.co` نرمال جواب می‌داد.
۴. بعد از اینکه کاربر داشبورد را باز کرد، پروژه شروع به restore شدن کرد:
   `521` → `404` → `503` → **`200`** (حدودِ ۷۵ ثانیه).

> **نکته‌ی مهم برای دفعه‌ی بعد:** تشخیصِ اولیه «پروژه حذف شده» **غلط** بود.
> Supabase رکوردِ DNS پروژه‌های *pause شده* را هم برمی‌دارد، پس NXDOMAIN دلیلِ حذف نیست.
> داده‌ها سرِ جایشان ماندند (۲۹ بازی از ۲ تا ۱۱ اوت هنوز در دیتابیس هستند) و
> نه کلیدها عوض شدند، نه `schema.sql` دوباره اجرا شد.

### تأییدِ نهایی
هر ۷ جدول (`games`, `players`, `roles`, `night_actions`, `day_votes`, `letters`,
`events_log`) با `HTTP 200` جواب دادند. سپس **همان دو `INSERT`ی که `tjCreateGame()`
می‌زند** سرتاسری تست شد (ساختِ اتاق + ثبتِ گرداننده) — هر دو موفق. رکوردهای تستی
بلافاصله پاک شدند (`HTTP 204`) و دیتابیس دست‌نخورده ماند.

جدولِ `roles` خالی است ولی بی‌ضرر — اپ اصلاً از آن کوئری نمی‌گیرد و نقش‌ها از
دیتای inline داخلِ `index.html` می‌آیند.

### تغییراتی که زده شد
هدف: اینکه دفعه‌ی بعد یک بک‌اندِ خواب، شبیهِ یک دکمه‌ی خراب به‌نظر نرسد.

**`supabase-client.js`**
- گاردِ صریح روی `window.supabase`. بدونِ آن، اگر CDN فیلتر/قطع باشد کلِ فایل در
  همان خط می‌ترکد و **همه‌ی** توابعِ `tj*` بی‌صدا تعریف‌نشده می‌مانند — یعنی تمامِ
  دکمه‌ها بی‌اثر می‌شوند بدونِ هیچ نشانه‌ای.
- تمامِ درخواست‌ها از یک `fetch` wrapper با تایم‌اوتِ ۱۵ ثانیه‌ای (`AbortController`)
  عبور می‌کنند تا بک‌اندِ در‌دسترس‌نبودن، سریع و با پیغامِ فارسی شکست بخورد نه با انتظارِ بی‌پایان.

**`index.html`**
- افزودنِ نوارِ وضعیتِ شناور (`.busy-box`) با پیغامِ «در حالِ ساختِ اتاق…».
- تبدیلِ `.err-box` به توستِ `position:fixed` با پس‌زمینه‌ی مات. دلیل: صفحه‌ی ورود
  یک تصویرِ بسیار بلند است و کادرِ خطا در جریانِ عادیِ صفحه، روی موبایل از کادرِ دید بیرون می‌افتاد.
- حذفِ نوشتنِ `textContent` روی `#createBtn` و `#joinBtn`. این دو هات‌اسپاتِ **نامرئی**
  روی تصویرند و تزریقِ متن در آن‌ها طرح را خراب می‌کرد.
- افزودنِ `tjBackendReady()` به‌عنوانِ پیش‌بررسی، تا نبودِ فایلِ کلاینت خودش را گزارش کند.

هر دو فایل با `node --check` سینتکس‌چک شدند.

### کارهای باز
- **ادغامِ فازِ شب:** موتورِ کاملِ `resolveNight.js` و کنسولِ کاملِ
  `host-night-console-prototype.html` هر دو نوشته شده‌اند ولی هنوز داخلِ `index.html`
  فراخوانی نمی‌شوند (آنجا فعلاً فقط `screen-night-wait` ایستا و پنلِ جاماسپ هست).
  سه قدمِ لازم: انتقالِ کنسول به `index.html`، تبدیلِ `resolveNight.js` از ESM به
  اسکریپتِ سراسری، و نوشتنِ اکشن‌ها در `night_actions` + ذخیره‌ی خروجی در `events_log`.
- **جلوگیری از pause شدنِ دوباره:** پروژه‌های رایگانِ Supabase بعد از ۷ روز
  بی‌فعالیتی خودکار pause می‌شوند.

</div>
